The patch program has been installed on this machine.

     NAME
	  patch	- a program for	applying a diff	file to	an original

     SYNOPSIS
	  patch	[options] orig patchfile [+ [options] orig]

	  but usually just

	  patch	<patchfile

     DESCRIPTION

	  Patch	will take a patch file containing a ... difference listing
	  produced by the diff program and apply those differences to an
	  original file, producing a patched version.  By default, the
	  patched version is put in place of the original, with the original
	  file backed up ...

See the manual page for more info.

					Tony Hansen
			    hansen@pegasus.att.com, tony@attmail.com
				att!pegasus!hansen, attmail!tony
