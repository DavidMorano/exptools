	vsh version 3.36

Symbolic links are displayed as "->" followed by the linked to file,
similar to ls -l, on exptools machine types where all operating system
releases support them: alliant, hppa, mips, sparc, and sun3.

Fixed a bug in where the O command didn't work the second time on sparc.

	Joe Steffen
