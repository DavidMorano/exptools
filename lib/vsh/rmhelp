To select a file for removal, press its corresponding letter (a-z).
If no letters are displayed, click mouse button 1 on the file.
Selecting a file a second time "undeletes" the file.

One can select all files on a page by pressing asterisk (*).

A file selected for removal is marked with "//".

One can move through the various pages with the usual commands
(0123456789+- ).

To exit without removing any files, type ^D or ^Z.
To exit and cause permanent removal of selected files, type R.

