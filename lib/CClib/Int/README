The C++ large integer package uses two source files,
Int.h and Int.c. To build the package, use
	CC -c -I. Int.c
To make it into a library, use
	ar c libInt.a Int.o
followed by ranlib on BSD systems.

The file Int.man contains a man page in troff format.

If you have any problems or questions,
send them to:

	Emden Gansner
	ulysses!erg
	MH x3864
