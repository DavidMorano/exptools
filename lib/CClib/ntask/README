This is a consolidation of the task source into a single directory.
The makefile must be edited for the local machine type and operating system.
See the macros SYS, BSD, and MACH in the makefile.  BSD must be defined
when it is necessary to execute 'ranlib' on an archive.
